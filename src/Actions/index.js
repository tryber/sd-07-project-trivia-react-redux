import { userNameAction, USER_NAME } from './user';
import {
  REQUEST,
  SUCCESS,
  ERROR,
  fetchGravatar,
} from './gravatar';
import {
  UPDATE_SCORE,
  RESET_SCORE,
  updateScoreAction,
  resetScoreAction,
} from './score';

export default {
  userNameAction,
  USER_NAME,
  REQUEST,
  SUCCESS,
  ERROR,
  fetchGravatar,
  UPDATE_SCORE,
  RESET_SCORE,
  updateScoreAction,
  resetScoreAction,
};
