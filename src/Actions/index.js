import { userNameAction, USER_NAME } from './user';
import {
  REQUEST,
  SUCCESS,
  ERROR,
  fetchGravatar,
} from './gravatar';

export default {
  userNameAction,
  USER_NAME,
  REQUEST,
  SUCCESS,
  ERROR,
  fetchGravatar,
};
